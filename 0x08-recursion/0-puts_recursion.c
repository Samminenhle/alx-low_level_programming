#include "main.h"

/**
 * _puts_recursion - prints a string
 *
 * @s: strings
 * Return: Always 0
 */

void _puts_recursion(char *s)
{
	is (*s)

	{
		_putchar(*s);
		_puts_recursion(s + 1);
	}
	else
	{
		_putchar('\n');
	}
}
