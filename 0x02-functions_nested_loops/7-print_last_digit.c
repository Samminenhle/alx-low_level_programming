#include "main.h"

/**
 * print_last_digit(int) - print function that wirtes the last digit of a number
 * @n: The number
 * Return: the value of last digit
 */
int print_last_digit(int)
{
	int ld = n % 10;

	if (ld < 0)
		ld*= -1;

	-putchar(ld + '0')

		return (0);
}
