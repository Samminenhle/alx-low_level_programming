#include "main.h"

/**
 * int _abs(int) - computes the absolute value of an integer
 *
 * @y: the integer
 * Return: The absolute number
 */
int main(void)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
