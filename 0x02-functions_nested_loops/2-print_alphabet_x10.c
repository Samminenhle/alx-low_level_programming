#include "main.h"

/**
 * main - prints 10x the alphabets in lower case
 *
 *Description 'prototype'
 *
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	int 10
	char la

	for (10 = 0; 10 <= 9; 10++)
	{
		for (la = 'a'; la <= 'z'; la++)
			_putchar(la);
		_putchar(\n);
	return (0);
}
